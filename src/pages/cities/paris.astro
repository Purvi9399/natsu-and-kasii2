---
title: "Paris"
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Paris · Natsu & Kasii</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Fonts: Parisian elegance -->
    <link
      href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,700;1,700&family=Merriweather:ital,wght@0,400;0,700&family=Parisienne&display=swap"
      rel="stylesheet"
    />

    <style>
      :root {
        --bg: #f7f3ed;
        --card: #ffffff;
        --text: #2a2730;
        --muted: #6f647d;
        --accent: #6f2e63;
        --accent2: #f8e5f2;
        --radius: 16px;
        --shadow: 0 38px 100px -15px rgba(111,46,99,0.22);
        --transition: 0.35s cubic-bezier(.3,.1,.25,1);
        --heading: "Cormorant Garamond", serif;
        --body: "Merriweather", system-ui,-apple-system,BlinkMacSystemFont,serif;
        --quote: "Parisienne", cursive;
      }

      * { box-sizing:border-box; }

      body {
        margin: 0;
        font-family: var(--body);
        background: var(--bg);
        color: var(--text);
        line-height:1.55;
        scroll-behavior: smooth;
        position: relative;
        min-height:100vh;
      }

      /* Eiffel Tower blurred background */
      body::before {
        content: "";
        position: fixed;
        inset: 0;
        background: url('/images/eiffel-blur.jpg') center/cover no-repeat fixed;
        filter: blur(14px) brightness(1.1);
        opacity: 0.13;
        z-index: 0;
        pointer-events: none;
        mix-blend-mode: normal;
      }

      a { color: var(--accent); text-decoration:none; }
      a:hover { text-decoration: underline; }

      .container {
        max-width: 1080px;
        margin: 0 auto;
        padding: 1.5rem 1rem 5rem;
        position: relative;
        z-index:1;
      }

      .back-btn {
        position: fixed;
        top: 16px;
        right: 16px;
        background: rgba(255,255,255,0.9);
        padding: 8px 14px;
        border-radius: 999px;
        font-size: 0.9rem;
        font-weight:600;
        box-shadow: 0 18px 50px -10px rgba(111,46,99,0.2);
        backdrop-filter: blur(6px);
        z-index:1001;
      }
      .back-btn:hover { background:#fff; }

      .mini-nav {
        display:flex;
        gap:1rem;
        flex-wrap:wrap;
        justify-content:center;
        margin-bottom:1rem;
        position:sticky;
        top:10px;
        z-index:100;
        background: rgba(247,243,237,0.95);
        padding:8px 12px;
        border-radius: 12px;
        box-shadow: 0 12px 40px -10px rgba(111,46,99,0.15);
        font-size:0.9rem;
      }
      .mini-nav a {
        padding:6px 14px;
        border-radius:8px;
        font-weight:600;
        position:relative;
        color: var(--text);
        transition: var(--transition);
      }
      .mini-nav a.active,
      .mini-nav a:hover {
        background: var(--accent);
        color:#fff;
      }

      .hero {
        position: relative;
        text-align: center;
        padding: 2rem 0 1rem;
        overflow: hidden;
      }
      .hero h1 {
        font-family: var(--heading);
        font-size: 3.8rem;
        margin: 0;
        color: var(--accent);
        position: relative;
        z-index:2;
      }
      .subtitle {
        margin: 8px auto 12px;
        max-width: 700px;
        font-size: 1.1rem;
        color: var(--muted);
        position: relative;
        z-index:2;
      }
      .tagline {
        margin:4px auto 16px;
        font-size:0.95rem;
        max-width:700px;
        color:#4f4568;
      }
      .quote {
        font-style: italic;
        margin: 10px auto 6px;
        max-width: 700px;
        position: relative;
        font-size:1rem;
        color: #4d4365;
      }
      .quote small {
        display:block;
        margin-top:4px;
        font-size:0.75rem;
        font-weight:600;
        color: #7a7284;
      }

      /* Ambient drift (soft rose/gold) */
      .ambient {
        pointer-events:none;
        position:absolute;
        inset:0;
        z-index:1;
        overflow:hidden;
      }
      .petal {
        position:absolute;
        width: 90px;
        height: 90px;
        background: radial-gradient(circle at 30% 30%, rgba(248,229,242,0.45), transparent 65%);
        border-radius:50%;
        filter: blur(20px);
        mix-blend-mode: screen;
        animation: drift 20s ease-in-out infinite;
        opacity:0.55;
      }
      @keyframes drift {
        0% { transform: translate(0,0) scale(1); }
        50% { transform: translate(15px,-28px) scale(1.07); }
        100% { transform: translate(0,0) scale(1); }
      }

      .section {
        margin: 3.5rem 0;
        position: relative;
      }
      .section h2 {
        font-family: var(--heading);
        font-size:2.6rem;
        margin-bottom:6px;
        display:inline-block;
        position: relative;
      }
      .section h2::after {
        content:"";
        display:block;
        height:4px;
        width:60px;
        background: var(--accent);
        border-radius:2px;
        margin-top:6px;
      }
      .section .desc {
        margin:8px 0 18px;
        max-width:820px;
        color: var(--muted);
      }

      .grid { display:grid; gap:1.25rem; }

      .card {
        background: var(--card);
        border-radius: var(--radius);
        padding:1.3rem 1.55rem;
        position:relative;
        overflow:hidden;
        box-shadow: var(--shadow);
        transition: var(--transition);
        display:flex;
        flex-direction:column;
        min-height:170px;
        opacity:0;
        transform: translateY(15px);
      }
      .card.visible { opacity:1; transform: translateY(0); }
      .card:hover {
        transform: translateY(-4px);
        box-shadow: 0 45px 110px -15px rgba(111,46,99,0.35);
      }
      .card h3 {
        margin:0 0 6px;
        font-family: var(--heading);
        font-size:1.4rem;
      }
      .card p { margin:0; font-size:0.95rem; color:#4d4365; flex:1; }
      .label {
        display:inline-block;
        background: var(--accent2);
        color:#4b3f5f;
        padding:4px 10px;
        border-radius:999px;
        font-size:0.6rem;
        font-weight:600;
        margin-bottom:6px;
      }

      .pill-list {
        display:flex;
        gap:12px;
        flex-wrap:wrap;
        margin:12px 0 6px;
      }
      .pill {
        background: var(--accent);
        color:white;
        padding:10px 18px;
        border-radius:999px;
        font-weight:600;
        font-size:0.85rem;
      }

      /* Flip postcards */
      .postcards {
        display:flex;
        gap:18px;
        flex-wrap:wrap;
        margin:2rem 0 1rem;
        justify-content:center;
      }
      .flip-card {
        background: transparent;
        width: 300px;
        height: 200px;
        perspective: 1000px;
        position: relative;
      }
      .flip-inner {
        position: relative;
        width: 100%;
        height: 100%;
        text-align: left;
        transition: transform 0.8s;
        transform-style: preserve-3d;
        border-radius: 14px;
      }
      .flip-card:hover .flip-inner { transform: rotateY(180deg); }
      .flip-front,
      .flip-back {
        position: absolute;
        width:100%;
        height:100%;
        backface-visibility: hidden;
        border-radius:14px;
        overflow:hidden;
        box-shadow: 0 28px 60px -10px rgba(0,0,0,0.08);
        display:flex;
        flex-direction:column;
        justify-content:flex-end;
        padding:14px;
        font-size:0.85rem;
      }
      .flip-front img {
        position:absolute;
        inset:0;
        width:100%;
        height:100%;
        object-fit: cover;
        filter: brightness(1);
      }
      .flip-front .overlay {
        position: relative;
        z-index:2;
        background: rgba(0,0,0,0.2);
        padding:8px 12px;
        border-radius:10px;
        color:#fff;
        backdrop-filter: blur(4px);
      }
      .flip-back {
        background: var(--card);
        transform: rotateY(180deg);
        padding:18px;
        display:flex;
        flex-direction:column;
      }
      .flip-back .title {
        margin:0;
        font-family: var(--heading);
        font-size:1rem;
      }
      .flip-back .note {
        margin:6px 0 0;
        font-size:0.75rem;
        flex:1;
        color:#4d4365;
      }

      /* Filters */
      .todo-filters {
        display:flex;
        gap:10px;
        flex-wrap:wrap;
        margin:10px 0 16px;
      }
      .todo-filters button {
        background: rgba(255,255,255,0.15);
        border:1px solid rgba(111,46,99,0.5);
        padding:8px 16px;
        border-radius:999px;
        cursor:pointer;
        color: var(--text);
        font-weight:600;
        transition: var(--transition);
        backdrop-filter: blur(6px);
      }
      .todo-filters button.active {
        background: var(--accent);
        color:#fff;
        border-color: var(--accent);
      }
      .todo-grid {
        display:grid;
        gap:1.25rem;
        grid-template-columns: repeat(auto-fit,minmax(240px,1fr));
      }
      .todo-card {
        position:relative;
        background: var(--card);
        border-radius: var(--radius);
        padding:1.25rem 1.5rem;
        box-shadow: var(--shadow);
        display:flex;
        flex-direction:column;
        transition: var(--transition);
        min-height:170px;
        opacity:0;
        transform: translateY(15px);
      }
      .todo-card.visible { opacity:1; transform:translateY(0); }
      .todo-card:hover { transform: translateY(-3px); }
      .todo-card h3 { margin:0 0 6px; font-family: var(--heading); font-size:1.2rem; }
      .todo-card small { margin-top:auto; font-size:0.65rem; opacity:.75; }

      .save-btn {
        position: absolute;
        top: 12px;
        right: 12px;
        background: rgba(255,255,255,0.95);
        border-radius:50%;
        width:32px;
        height:32px;
        display:flex;
        align-items:center;
        justify-content:center;
        cursor:pointer;
        border:1px solid rgba(111,46,99,0.4);
        transition: var(--transition);
        padding:0;
      }
      .save-btn svg {
        width: 16px;
        height: 16px;
        stroke: var(--accent);
        fill: none;
        transition: var(--transition);
      }
      .save-btn.saved {
        background: var(--accent);
        border-color: var(--accent);
      }
      .save-btn.saved svg {
        stroke: white;
        fill: white;
      }

      /* Itinerary toolbar */
      .itinerary-toolbar {
        position: fixed;
        bottom: 20px;
        right: 20px;
        background: #fff;
        border-radius: 14px;
        padding: 14px 18px;
        box-shadow: 0 40px 100px -10px rgba(111,46,99,0.3);
        display: flex;
        gap: 16px;
        align-items: flex-start;
        font-size: 0.85rem;
        z-index: 1200;
        max-width: 380px;
        flex-wrap: wrap;
        border:1px solid rgba(111,46,99,0.3);
      }
      .saved-list {
        display:flex;
        flex-wrap:wrap;
        gap:6px;
        margin-bottom:4px;
      }
      .saved-pill {
        background: rgba(111,46,99,0.1);
        padding: 4px 10px;
        border-radius:999px;
        font-size:0.6rem;
        display:flex;
        align-items:center;
        gap:4px;
      }
      .itinerary-actions {
        display:flex;
        gap:8px;
        flex-wrap:wrap;
      }
      .itinerary-actions button {
        background: var(--accent);
        border:none;
        color:white;
        padding:6px 12px;
        border-radius:999px;
        cursor:pointer;
        font-size:0.7rem;
        font-weight:600;
        transition: var(--transition);
      }
      .itinerary-actions button.secondary {
        background: #666;
      }
      @keyframes pill-bounce {
        0% { transform: scale(1); }
        30% { transform: scale(1.15); }
        60% { transform: scale(0.95); }
        100% { transform: scale(1); }
      }
      .saved-pill.bounce { animation: pill-bounce 0.6s ease; }

      .memory {
        display:grid;
        gap:24px;
        grid-template-columns: repeat(auto-fit,minmax(250px,1fr));
        margin:1rem 0 2rem;
      }
      .memory-text {
        background: var(--card);
        border-radius: var(--radius);
        padding:1.5rem;
        position: relative;
        box-shadow: var(--shadow);
      }
      .memory-text h3 {
        margin-top:0;
        font-family: var(--heading);
        font-size:1.6rem;
      }
      .memory-text p {
        margin:0.5rem 0 0;
        font-size:0.95rem;
      }

      .itinerary-box {
        background: var(--card);
        border-radius: var(--radius);
        padding:1.5rem 1.75rem;
        display:flex;
        flex-direction:column;
        gap:14px;
        box-shadow: var(--shadow);
        position:relative;
      }
      .itinerary-box h2 {
        margin:0;
        font-family: var(--heading);
        font-size:2rem;
      }
      .field {
        flex:1;
        min-width:140px;
        display:flex;
        flex-direction:column;
      }
      .field label {
        font-size:0.65rem;
        font-weight:600;
        margin-bottom:4px;
        text-transform:uppercase;
        letter-spacing:0.8px;
        color:#666;
      }
      .field select,
      .field input {
        padding:10px 12px;
        border-radius:8px;
        border:1px solid #ddd;
        font-size:0.95rem;
        outline:none;
      }
      .btn {
        background: var(--accent);
        color:white;
        border:none;
        padding:12px 18px;
        border-radius:999px;
        cursor:pointer;
        font-weight:600;
        font-size:1rem;
        transition: var(--transition);
      }
      .btn:hover { filter:brightness(1.05); }

      @media (max-width:960px) {
        .grid { grid-template-columns:1fr; }
        .memory { grid-template-columns:1fr; }
      }
    </style>
  </head>

  <body>
    <a class="back-btn" href="/">← Back</a>

    <div class="container">
      <!-- Mini TOC -->
      <div class="mini-nav" aria-label="section navigation">
        <a href="#best-time" data-target="best-time">Best Time</a>
        <a href="#dont-miss" data-target="dont-miss">Don't Miss</a>
        <a href="#postcards" data-target="postcards">Postcards</a>
        <a href="#memory" data-target="memory">Paris Memory</a>
        <a href="#things-to-do" data-target="things-to-do">Things to Do</a>
        <a href="#best-views" data-target="best-views">Best Views</a>
        <a href="#cafes" data-target="cafes">Café Corners</a>
        <a href="#nightlife" data-target="nightlife">Nightlife</a>
        <a href="#offbeat" data-target="offbeat">Offbeat</a>
        <a href="#itinerary" data-target="itinerary">Custom Itinerary</a>
      </div>

      <!-- Hero / Intro -->
      <section class="hero" id="hero">
        <div class="ambient" aria-hidden="true">
          <div class="petal" style="top:10%; left:20%; animation-delay:0s;"></div>
          <div class="petal" style="top:55%; left:75%; animation-delay:3s;"></div>
          <div class="petal" style="top:70%; left:40%; animation-delay:6s;"></div>
        </div>

        <h1>Paris</h1>
        <div class="subtitle">
          Light that lingers, boulevards that breathe, and rituals that feel personal. Paris is a
          city of layers—each one invites you to slow down and taste it.
        </div>
        <div class="quote">
          “In Paris you don’t visit a place, you inhale its stories.” <small>— Local whisper</small>
        </div>
        <div class="quote">
          “The city gives you time back, if you let it.” <small>— Ayushi</small>
        </div>
      </section>

      <!-- Best Time to Visit -->
      <section class="section" id="best-time">
        <h2>Best Time to Visit</h2>
        <div class="desc">
          There’s no single “perfect” season in Paris—each has its own poetry. Spring blushes the
          terraces, summer hums with café life, winter wraps streets in a golden hush, and autumn
          softens light into quiet frames. If it’s your first time, we recommend late spring or
          early autumn: mild days, blooming corners, and just enough warmth to linger without the
          rush. 
        </div>
        <div class="card" style="max-width:850px; margin:0 auto; display:flex; gap:20px; flex-wrap:wrap;">
          <div style="flex:1; min-width:220px;">
            <div class="label">Why First-Timers</div>
            <h3 style="margin-top:0; font-size:1.35rem;">Harmony of light & ease</h3>
            <p>
              Late spring brings blossoms and softer crowds; early fall layers mellow light over
              streets, making every stroll feel like an unforced discovery.
            </p>
          </div>
          <div style="flex:1; min-width:220px;">
            <div class="label">Seasonal Notes</div>
            <h3 style="margin-top:0; font-size:1.35rem;">Every vibe exists here</h3>
            <p>
              Summer is lively and long; winter is intimate and golden; each visit writes a different
              story—so you can always come back.
            </p>
          </div>
        </div>
      </section>

      <!-- Don't Miss -->
      <section class="section" id="dont-miss">
        <h2>Don't Miss</h2>
        <div class="desc">
          Core anchors that a local would start with—places that orient the rhythm and soul of Paris.
        </div>
        <div class="pill-list">
          <div class="pill">Eiffel Tower</div>
          <div class="pill">Louvre</div>
          <div class="pill">Sainte-Chapelle</div>
          <div class="pill">Montmartre</div>
          <div class="pill">Seine Stroll</div>
        </div>
        <div class="grid" style="grid-template-columns:repeat(auto-fit,minmax(240px,1fr)); margin-top:8px;">
          <div class="card" data-id="eiffel-tower">
            <div class="save-btn" aria-label="save Eiffel Tower">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <div class="label">Iconic</div>
            <h3>Eiffel Tower</h3>
            <p>
              The silhouette that anchors every memory—best around golden hour, then linger in the
              nearby gardens for reflection.
            </p>
          </div>
          <div class="card" data-id="louvre">
            <div class="save-btn" aria-label="save Louvre">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <div class="label">Cultural</div>
            <h3>Louvre</h3>
            <p>
              Beyond the headline pieces—lose yourself in quiet wings and discover smaller
              treasures that feel like secrets.
            </p>
          </div>
          <div class="card" data-id="sainte-chapelle">
            <div class="save-btn" aria-label="save Sainte-Chapelle">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <div class="label">Stained Light</div>
            <h3>Sainte-Chapelle</h3>
            <p>
              A chapel of light—when the sun pours through, the colors feel alive, like living glass.
            </p>
          </div>
          <div class="card" data-id="montmartre">
            <div class="save-btn" aria-label="save Montmartre">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <div class="label">Artist’s Hill</div>
            <h3>Montmartre</h3>
            <p>
              Cobble-wrapped viewpoints and creative corners—each step feels like a brushstroke.
            </p>
          </div>
          <div class="card" data-id="seine-stroll">
            <div class="save-btn" aria-label="save Seine Stroll">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <div class="label">Riverside</div>
            <h3>Seine Stroll</h3>
            <p>
              Wander the quays early; Paris unfolds its stories best when you walk beside its river.
            </p>
          </div>
        </div>
      </section>

      <!-- Postcards -->
      <section class="section" id="postcards">
        <h2>Postcards</h2>
        <div class="desc">
          Snippets from our Paris wanderings—little notes that feel like insider postcards.
        </div>
        <div class="postcards">
          <div class="flip-card">
            <div class="flip-inner">
              <div class="flip-front">
                <img src="/images/paris-sunset.jpg" alt="Sunset on Seine" />
                <div class="overlay">
                  <p class="title" style="margin:0;">Seine Sunset</p>
                  <p class="note" style="margin:4px 0 0; font-size:0.7rem;">
                    Light pooling into the river, boats drifting like soft breaths.
                  </p>
                </div>
              </div>
              <div class="flip-back">
                <div class="title">Seine Sunset</div>
                <div class="note">
                  Shared pastries by the quay as color softened—Paris felt like it sighed with us.
                  —Purvi
                </div>
              </div>
            </div>
          </div>

          <div class="flip-card">
            <div class="flip-inner">
              <div class="flip-front">
                <img src="/images/paris-cafe.jpg" alt="Corner Café" />
                <div class="overlay">
                  <p class="title" style="margin:0;">Corner Café Pause</p>
                  <p class="note" style="margin:4px 0 0; font-size:0.7rem;">
                    Coffee, soft chatter, and unhurried minutes.
                  </p>
                </div>
              </div>
              <div class="flip-back">
                <div class="title">Corner Café Pause</div>
                <div class="note">
                  Watched people drift by while we scribbled—small rituals became the memory. —
                  Ayushi
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Paris Memory -->
      <section class="section" id="memory">
        <h2>Paris Memory</h2>
        <div class="desc">
          Real moments that lived outside the plan—unexpected discoveries, quiet evenings, and
          the kind of smallness that feels generous.
        </div>
        <div class="memory">
          <div class="flip-card" style="max-width:320px;">
            <div class="flip-inner">
              <div class="flip-front">
                <img src="/images/paris-rain.jpg" alt="Rainy Alley" />
                <div class="overlay">
                  <p class="title" style="margin:0;">Rainy Alley</p>
                  <p class="note" style="margin:4px 0 0; font-size:0.7rem;">
                    Umbrellas, wet stone, and jazz spilling from an unmarked door.
                  </p>
                </div>
              </div>
              <div class="flip-back">
                <div class="title">Rainy Alley</div>
                <div class="note">
                  The rain softened everything—ended up in a tiny jazz bar we didn’t plan to find.
                  —Purvi
                </div>
              </div>
            </div>
          </div>

          <div class="memory-text">
            <h3>Unscripted Evening</h3>
            <p>
              A closed museum sent us wandering; we found a basement wine spot where the owner
              poured stories with each glass. Paris rewarded deviation.
            </p>
            <p style="margin-top:8px; font-style:italic;">
              “The best parts are the ones you don’t schedule.” — Ayushi
            </p>
          </div>

          <div class="flip-card" style="max-width:320px;">
            <div class="flip-inner">
              <div class="flip-front">
                <img src="/images/paris-midnight.jpg" alt="Night Lights" />
                <div class="overlay">
                  <p class="title" style="margin:0;">Midnight Lights</p>
                  <p class="note" style="margin:4px 0 0; font-size:0.7rem;">
                    Boulevards quiet, lampposts glowing, and the city feeling utterly personal.
                  </p>
                </div>
              </div>
              <div class="flip-back">
                <div class="title">Midnight Lights</div>
                <div class="note">
                  Wandering after dinner, the silence had a heartbeat—Paris felt like ours. —
                  Purvi & Ayushi
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Things to Do -->
      <section class="section" id="things-to-do">
        <h2>Things to Do</h2>
        <div class="desc">
          Local-tested layers—start with anchors, then drift into corners that feel like your own.
          Everything here feeds into a well-curated Paris itinerary (free for you).
        </div>

        <div class="todo-filters" aria-label="filters for things to do">
          <button data-filter="all" class="active">All</button>
          <button data-filter="must">Must Visit</button>
          <button data-filter="views">Best Views</button>
          <button data-filter="cafes">Cafés</button>
          <button data-filter="nightlife">Nightlife</button>
          <button data-filter="offbeat">Offbeat</button>
        </div>

        <div class="todo-grid">
          <!-- Must Visit -->
          <div class="todo-card" data-category="must" data-id="eiffel-tower">
            <div class="save-btn" aria-label="save Eiffel Tower">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Eiffel Tower</h3>
            <p>Symbolic ascent & evening glow—Paris’s heartbeat in vertical form.</p>
            <small>Landmark</small>
          </div>
          <div class="todo-card" data-category="must" data-id="louvre">
            <div class="save-btn" aria-label="save Louvre">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Louvre</h3>
            <p>Wander its wings and discover unexpected corners beyond the famous faces.</p>
            <small>Museum</small>
          </div>
          <div class="todo-card" data-category="must" data-id="sainte-chapelle">
            <div class="save-btn" aria-label="save Sainte-Chapelle">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Sainte-Chapelle</h3>
            <p>Stained-glass light that feels like falling into poetry.</p>
            <small>Spiritual</small>
          </div>

          <!-- Best Views -->
          <div class="todo-card" data-category="views" data-id="montmartre-view">
            <div class="save-btn" aria-label="save Montmartre View">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Montmartre</h3>
            <p>Panoramic sweep—feels like looking out over a painted Paris.</p>
            <small>Vantage</small>
          </div>
          <div class="todo-card" data-category="views" data-id="arc-de-triomphe-view">
            <div class="save-btn" aria-label="save Arc de Triomphe">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Arc de Triomphe</h3>
            <p>Classic boulevard & symmetry view from above.</p>
            <small>City view</small>
          </div>

          <!-- Cafés -->
          <div class="todo-card" data-category="cafes" data-id="cafe-de-flore">
            <div class="save-btn" aria-label="save Café de Flore">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Café de Flore</h3>
            <p>Terrace people-watching and rich coffee—ritual in slow motion.</p>
            <small>Classic</small>
          </div>
          <div class="todo-card" data-category="cafes" data-id="la-maison-rose">
            <div class="save-btn" aria-label="save La Maison Rose">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>La Maison Rose</h3>
            <p>Pastel charm, quiet corners—journal with a café au lait.</p>
            <small>Picturesque</small>
          </div>

          <!-- Nightlife -->
          <div class="todo-card" data-category="nightlife" data-id="rex-club">
            <div class="save-btn" aria-label="save Rex Club">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Rex Club</h3>
            <p>Late-night electronic pulse—Parisians slip in for curated rhythms.</p>
            <small>Underground</small>
          </div>
          <div class="todo-card" data-category="nightlife" data-id="little-red-door">
            <div class="save-btn" aria-label="save Little Red Door">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Little Red Door</h3>
            <p>Hidden cocktail artistry—subtle entrance, layered flavors, intimate ambiance.</p>
            <small>Creative</small>
          </div>

          <!-- Offbeat -->
          <div class="todo-card" data-category="offbeat" data-id="musee-chasse">
            <div class="save-btn" aria-label="save Musée de la Chasse">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Musée de la Chasse</h3>
            <p>Odd aristocratic narratives—taxidermy turned into storytelling art.</p>
            <small>Quirky</small>
          </div>
          <div class="todo-card" data-category="offbeat" data-id="coulee-verte">
            <div class="save-btn" aria-label="save Coulée Verte">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Coulée Verte</h3>
            <p>Elevated green path—secret calm for locals to reset above the bustle.</p>
            <small>Hidden</small>
          </div>
          <div class="todo-card" data-category="offbeat" data-id="canal-saint-martin">
            <div class="save-btn" aria-label="save Canal Saint-Martin">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Canal Saint-Martin</h3>
            <p>Slow local rhythm—picnics, iron bridges, and soft stretching days.</p>
            <small>Offbeat</small>
          </div>
        </div>
      </section>

      <!-- Custom Itinerary -->
      <section class="section" id="itinerary">
        <h2>Your Custom Paris Itinerary</h2>
        <div class="desc">
          Handcrafted from what you’ve saved—“This is your Paris mini-itinerary for today,”
          numbered and recommended by Natsu & Kasii. Well-curated, personalized, and completely
          free. Pick your vibe, group size, and dates; we’ll package it for you via WhatsApp.
        </div>
        <div class="itinerary-box">
          <div style="display:flex; gap:18px; flex-wrap:wrap;">
            <div class="field">
              <label for="vibe">Vibe</label>
              <select id="vibe">
                <option value="romantic">Romantic</option>
                <option value="art">Artful Wandering</option>
                <option value="cafe">Café Leisure</option>
                <option value="night">Night Pulse</option>
                <option value="mixed">Mixed</option>
              </select>
            </div>
            <div class="field">
              <label for="people">Group Size</label>
              <input type="number" id="people" min="1" value="2" />
            </div>
            <div class="field">
              <label for="dates">Dates (optional)</label>
              <input type="text" id="dates" placeholder="e.g., 4–8 Oct" />
            </div>
          </div>
          <div>
            <p style="margin:6px 0;">
              We’ll auto-generate a neat numbered itinerary based on what you’ve saved and the vibe
              you choose. Shareable, light, and free—no signup needed.
            </p>
            <button class="btn" id="whatsapp-paris">Send on WhatsApp</button>
          </div>
        </div>
      </section>
    </div>

    <!-- Toolbar -->
    <div id="itinerary-toolbar" class="itinerary-toolbar" style="display:none;">
      <div style="flex:1; min-width:140px;">
        <h4>Your Paris mini-itinerary</h4>
        <div id="itinerary-summary" style="margin-bottom:6px; font-size:0.9rem; line-height:1.2;"></div>
        <div class="saved-list" id="saved-list"></div>
        <div style="font-size:0.6rem; color:#555;">
          Build, copy, or share what you’ve saved.
        </div>
      </div>
      <div class="itinerary-actions">
        <button id="copy-link">Copy Link</button>
        <button id="share-whatsapp">Share on WhatsApp</button>
        <button id="share-email" class="secondary">Email</button>
        <button id="clear-itinerary" class="secondary">Clear</button>
      </div>
    </div>

    <script>
      const DISPLAY_NAMES = {
        "eiffel-tower": "Eiffel Tower",
        "louvre": "Louvre",
        "sainte-chapelle": "Sainte-Chapelle",
        "montmartre": "Montmartre",
        "seine-stroll": "Seine Stroll",
        "montmartre-view": "Montmartre",
        "arc-de-triomphe-view": "Arc de Triomphe",
        "cafe-de-flore": "Café de Flore",
        "la-maison-rose": "La Maison Rose",
        "rex-club": "Rex Club",
        "little-red-door": "Little Red Door",
        "musee-chasse": "Musée de la Chasse",
        "coulee-verte": "Coulée Verte",
        "canal-saint-martin": "Canal Saint-Martin"
      };
      function getDisplayName(id) {
        return DISPLAY_NAMES[id] || id.replace(/[-_]/g, " ");
      }

      const STORAGE_KEY = "paris-saved-items";
      let previousSaved = [];

      function getSaved() {
        try {
          const raw = localStorage.getItem(STORAGE_KEY);
          return raw ? JSON.parse(raw) : [];
        } catch { return []; }
      }
      function setSaved(arr) {
        localStorage.setItem(STORAGE_KEY, JSON.stringify(arr));
        refreshToolbar();
      }
      function toggleSaved(id) {
        let saved = getSaved();
        if (saved.includes(id)) saved = saved.filter(s => s !== id);
        else saved.push(id);
        setSaved(saved);
        updateCardState();
      }
      function updateCardState() {
        const saved = getSaved();
        document.querySelectorAll(".save-btn").forEach(btn => {
          const parent = btn.closest("[data-id]");
          if (!parent) return;
          const id = parent.dataset.id;
          if (saved.includes(id)) btn.classList.add("saved");
          else btn.classList.remove("saved");
        });
      }
      function updateSummary() {
        const summaryEl = document.getElementById("itinerary-summary");
        const saved = getSaved();
        if (!saved.length) {
          summaryEl.textContent = "";
          return;
        }
        const names = saved.map((id, idx) => `${idx + 1}. ${getDisplayName(id)}`);
        const baseUrl = `${window.location.origin}${window.location.pathname}`;
        const param = encodeURIComponent(saved.join(","));
        const shareLink = `${baseUrl}?saved=${param}`;
        summaryEl.innerHTML = `
          <strong>Today's Paris itinerary:</strong> ${names.join(" · ")}.<br/>
          Recommended by <em>Natsu & Kasii</em>. 
          <a href="${shareLink}" style="color: var(--accent); text-decoration:underline;">View / share this</a>
        `;
      }
      function refreshToolbar() {
        const toolbar = document.getElementById("itinerary-toolbar");
        const saved = getSaved();
        const savedList = document.getElementById("saved-list");
        savedList.innerHTML = "";
        if (!saved.length) {
          toolbar.style.display = "none";
          previousSaved = [];
          updateSummary();
          return;
        }
        toolbar.style.display = "flex";
        const newlyAdded = saved.filter(id => !previousSaved.includes(id));
        saved.forEach(id => {
          const pill = document.createElement("div");
          pill.className = "saved-pill";
          pill.textContent = getDisplayName(id);
          const x = document.createElement("span");
          x.style.marginLeft = "6px";
          x.style.cursor = "pointer";
          x.textContent = "×";
          x.addEventListener("click", () => toggleSaved(id));
          pill.appendChild(x);
          if (newlyAdded.includes(id)) {
            pill.classList.add("bounce");
            pill.addEventListener("animationend", () => pill.classList.remove("bounce"));
          }
          savedList.appendChild(pill);
        });
        previousSaved = [...saved];
        updateSummary();
      }

      document.addEventListener("DOMContentLoaded", () => {
        // progressive reveal
        const observer = new IntersectionObserver((entries) => {
          entries.forEach(e => {
            if (e.isIntersecting) {
              e.target.classList.add("visible");
              observer.unobserve(e.target);
            }
          });
        }, { threshold: 0.15 });
        document.querySelectorAll(".card, .todo-card").forEach(el => observer.observe(el));

        // mini-TOC highlight
        const sectionObserver = new IntersectionObserver((entries) => {
          entries.forEach(entry => {
            const id = entry.target.id;
            const link = document.querySelector(`.mini-nav a[data-target="${id}"]`);
            if (link && entry.isIntersecting) {
              document.querySelectorAll(".mini-nav a").forEach(a => a.classList.remove("active"));
              link.classList.add("active");
            }
          });
        }, { rootMargin: "-40% 0px -50% 0px" });
        document.querySelectorAll(".section").forEach(s => sectionObserver.observe(s));

        // save listeners
        document.querySelectorAll(".save-btn").forEach(btn => {
          btn.addEventListener("click", (e) => {
            e.stopPropagation();
            const card = btn.closest("[data-id]");
            if (!card) return;
            toggleSaved(card.dataset.id);
          });
        });

        // filters
        document.querySelectorAll(".todo-filters button").forEach(b => {
          b.addEventListener("click", () => {
            document.querySelectorAll(".todo-filters button").forEach(x => x.classList.remove("active"));
            b.classList.add("active");
            const filter = b.dataset.filter;
            document.querySelectorAll(".todo-card").forEach(card => {
              if (filter === "all" || card.dataset.category === filter) card.style.display="flex";
              else card.style.display="none";
            });
          });
        });

        // restore saved from URL
        const params = new URLSearchParams(window.location.search);
        if (params.has("saved")) {
          try {
            const incoming = decodeURIComponent(params.get("saved") || "");
            const list = incoming.split(",").filter(Boolean);
            setSaved(Array.from(new Set([...getSaved(), ...list])));
          } catch {}
        } else {
          refreshToolbar();
        }
        updateCardState();
      });

      // share / copy
      document.getElementById("copy-link").addEventListener("click", () => {
        const saved = getSaved();
        if (!saved.length) return;
        const param = encodeURIComponent(saved.join(","));
        const url = `${window.location.origin}${window.location.pathname}?saved=${param}`;
        const names = saved.map((id, idx) => `${idx+1}. ${getDisplayName(id)}`).join("\n");
        const text = `Today's Paris itinerary:\n${names}\n\nRecommended by Natsu & Kasii\n${url}`;
        navigator.clipboard.writeText(text);
        alert("Itinerary copied!");
      });
      document.getElementById("share-whatsapp").addEventListener("click", () => {
        const saved = getSaved();
        if (!saved.length) return;
        const baseUrl = `${window.location.origin}${window.location.pathname}`;
        const param = encodeURIComponent(saved.join(","));
        const shareLink = `${baseUrl}?saved=${param}`;
        const names = saved.map((id, idx) => `${idx+1}. ${getDisplayName(id)}`).join("\n");
        const summary = `My Paris mini-itinerary:\n${names}\n\nRecommended by Natsu & Kasii\n${shareLink}`;
        window.open(`https://wa.me/?text=${encodeURIComponent(summary)}`, "_blank");
      });
      document.getElementById("share-email").addEventListener("click", () => {
        const saved = getSaved();
        if (!saved.length) return;
        const param = encodeURIComponent(saved.join(","));
        const url = `${window.location.origin}${window.location.pathname}?saved=${param}`;
        window.location.href = `mailto:?subject=My Paris Mini-Itinerary&body=${encodeURIComponent("Here's what I saved:\n" + url)}`;
      });
      document.getElementById("clear-itinerary").addEventListener("click", () => {
        localStorage.removeItem(STORAGE_KEY);
        updateCardState();
        refreshToolbar();
      });

      // WhatsApp custom itinerary
      document.getElementById("whatsapp-paris").addEventListener("click", () => {
        const vibe = document.getElementById("vibe").value;
        const people = document.getElementById("people").value;
        const dates = document.getElementById("dates").value;
        const saved = getSaved();
        const namesList = saved.map((id, idx) => `${idx+1}. ${getDisplayName(id)}`).join("\n");
        let msg = `This is your Paris mini-itinerary for today:\n${namesList}\n\nVibe: ${vibe}\nGroup size: ${people}`;
        if (dates.trim()) msg += `\nDates: ${dates}`;
        msg += `\nRecommended by Natsu & Kasii.`;
        const phone = "1234567890"; // replace with real number
        window.open(`https://wa.me/${phone}?text=${encodeURIComponent(msg)}`, "_blank");
      });
    </script>
  </body>
</html>
