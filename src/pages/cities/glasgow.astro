---
title: "Glasgow"
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Glasgow · Natsu & Kasii</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Fonts: sturdy heritage + modern clarity -->
    <link
      href="https://fonts.googleapis.com/css2?family=Rokkitt:ital,wght@0,700;1,700&family=Inter:ital,wght@0,400;0,600&family=Dancing+Script&display=swap"
      rel="stylesheet"
    />

    <style>
      :root {
        --bg: #eef3f7;
        --card: #ffffff;
        --text: #23272b;
        --muted: #6d6f7a;
        --accent: #1f3b4d;
        --accent2: #d98c3f;
        --radius: 14px;
        --shadow: 0 38px 110px -15px rgba(31,59,77,0.25);
        --transition: 0.35s cubic-bezier(.3,.1,.25,1);
        --heading: "Rokkitt", serif;
        --body: "Inter", system-ui,-apple-system,BlinkMacSystemFont,sans-serif;
        --quote: "Dancing Script", cursive;
      }

      * { box-sizing:border-box; }

      body {
        margin: 0;
        font-family: var(--body);
        background: var(--bg);
        color: var(--text);
        line-height:1.55;
        scroll-behavior: smooth;
        position: relative;
        min-height:100vh;
      }

      /* Glasgow skyline / Clyde blurred background */
      body::before {
        content: "";
        position: fixed;
        inset: 0;
        background: url('/images/glasgow-blur.jpg') center/cover no-repeat fixed;
        filter: blur(14px) brightness(1.1);
        opacity: 0.12;
        z-index: 0;
        pointer-events: none;
        mix-blend-mode: normal;
      }

      a { color: var(--accent); text-decoration:none; }
      a:hover { text-decoration: underline; }

      .container {
        max-width: 1080px;
        margin: 0 auto;
        padding: 1.5rem 1rem 5rem;
        position: relative;
        z-index:1;
      }

      .back-btn {
        position: fixed;
        top: 16px;
        right: 16px;
        background: rgba(255,255,255,0.9);
        padding: 8px 14px;
        border-radius: 999px;
        font-size: 0.9rem;
        font-weight:600;
        box-shadow: 0 18px 50px -10px rgba(31,59,77,0.2);
        backdrop-filter: blur(6px);
        z-index:1001;
      }
      .back-btn:hover { background:#fff; }

      .mini-nav {
        display:flex;
        gap:1rem;
        flex-wrap:wrap;
        justify-content:center;
        margin-bottom:1rem;
        position:sticky;
        top:10px;
        z-index:100;
        background: rgba(238,243,247,0.95);
        padding:8px 12px;
        border-radius: 12px;
        box-shadow: 0 12px 40px -10px rgba(31,59,77,0.15);
        font-size:0.9rem;
      }
      .mini-nav a {
        padding:6px 14px;
        border-radius:8px;
        font-weight:600;
        position:relative;
        color: var(--text);
        transition: var(--transition);
      }
      .mini-nav a.active,
      .mini-nav a:hover {
        background: var(--accent);
        color:#fff;
      }

      .hero {
        position: relative;
        text-align: center;
        padding: 2rem 0 1rem;
        overflow: hidden;
      }
      .hero h1 {
        font-family: var(--heading);
        font-size: 3.8rem;
        margin: 0;
        color: var(--accent);
        position: relative;
        z-index:2;
      }
      .subtitle {
        margin: 8px auto 12px;
        max-width: 700px;
        font-size: 1.1rem;
        color: var(--muted);
        position: relative;
        z-index:2;
      }
      .quote {
        font-family: var(--quote);
        font-size: 1.1rem;
        margin: 10px auto 6px;
        max-width: 700px;
        position: relative;
        color: #3f3c44;
      }
      .quote small {
        display:block;
        margin-top:4px;
        font-size:0.65rem;
        font-weight:600;
        color: #7a7a8c;
        font-family: var(--body);
      }

      /* Ambient Glasgow mood: drifting soft geometric "rain" hints */
      .ambient {
        pointer-events:none;
        position:absolute;
        inset:0;
        z-index:1;
        overflow:hidden;
      }
      .drop {
        position:absolute;
        width:110px;
        height:110px;
        background: radial-gradient(circle at 50% 50%, rgba(217,140,63,0.28), transparent 65%);
        border-radius:50%;
        filter: blur(28px);
        mix-blend-mode: screen;
        animation: float 24s ease-in-out infinite;
        opacity:0.5;
      }
      @keyframes float {
        0% { transform: translate(0,0) scale(1); }
        50% { transform: translate(18px,-22px) scale(1.04); }
        100% { transform: translate(0,0) scale(1); }
      }

      .section {
        margin: 3.5rem 0;
        position: relative;
      }
      .section h2 {
        font-family: var(--heading);
        font-size:2.6rem;
        margin-bottom:6px;
        display:inline-block;
        position: relative;
      }
      .section h2::after {
        content:"";
        display:block;
        height:4px;
        width:60px;
        background: var(--accent);
        border-radius:2px;
        margin-top:6px;
      }
      .section .desc {
        margin:8px 0 18px;
        max-width:820px;
        color: var(--muted);
      }

      .grid { display:grid; gap:1.25rem; }

      .card {
        background: var(--card);
        border-radius: var(--radius);
        padding:1.3rem 1.55rem;
        position:relative;
        overflow:hidden;
        box-shadow: var(--shadow);
        transition: var(--transition);
        display:flex;
        flex-direction:column;
        min-height:170px;
        opacity:0;
        transform: translateY(15px);
      }
      .card.visible { opacity:1; transform: translateY(0); }
      .card:hover {
        transform: translateY(-4px);
        box-shadow: 0 45px 110px -15px rgba(31,59,77,0.35);
      }
      .card h3 {
        margin:0 0 6px;
        font-family: var(--heading);
        font-size:1.4rem;
      }
      .card p { margin:0; font-size:0.95rem; color:#3f3c44; flex:1; }
      .label {
        display:inline-block;
        background: var(--accent2);
        color:#3f3024;
        padding:4px 10px;
        border-radius:999px;
        font-size:0.6rem;
        font-weight:600;
        margin-bottom:6px;
      }

      .pill-list {
        display:flex;
        gap:12px;
        flex-wrap:wrap;
        margin:12px 0 6px;
      }
      .pill {
        background: var(--accent);
        color:white;
        padding:10px 18px;
        border-radius:999px;
        font-weight:600;
        font-size:0.85rem;
      }

      /* Flip postcards */
      .postcards {
        display:flex;
        gap:18px;
        flex-wrap:wrap;
        margin:2rem 0 1rem;
        justify-content:center;
      }
      .flip-card {
        background: transparent;
        width: 300px;
        height: 200px;
        perspective: 1000px;
        position: relative;
      }
      .flip-inner {
        position: relative;
        width: 100%;
        height: 100%;
        text-align: left;
        transition: transform 0.8s;
        transform-style: preserve-3d;
        border-radius: 14px;
      }
      .flip-card:hover .flip-inner { transform: rotateY(180deg); }
      .flip-front,
      .flip-back {
        position: absolute;
        width:100%;
        height:100%;
        backface-visibility: hidden;
        border-radius:14px;
        overflow:hidden;
        box-shadow: 0 28px 60px -10px rgba(0,0,0,0.08);
        display:flex;
        flex-direction:column;
        justify-content:flex-end;
        padding:14px;
        font-size:0.85rem;
      }
      .flip-front img {
        position:absolute;
        inset:0;
        width:100%;
        height:100%;
        object-fit: cover;
        filter: brightness(1);
      }
      .flip-front .overlay {
        position: relative;
        z-index:2;
        background: rgba(0,0,0,0.22);
        padding:8px 12px;
        border-radius:10px;
        color:#fff;
        backdrop-filter: blur(4px);
      }
      .flip-back {
        background: var(--card);
        transform: rotateY(180deg);
        padding:18px;
        display:flex;
        flex-direction:column;
      }
      .flip-back .title {
        margin:0;
        font-family: var(--heading);
        font-size:1rem;
      }
      .flip-back .note {
        margin:6px 0 0;
        font-size:0.75rem;
        flex:1;
        color:#3f3c44;
      }

      /* Filters */
      .todo-filters {
        display:flex;
        gap:10px;
        flex-wrap:wrap;
        margin:10px 0 16px;
      }
      .todo-filters button {
        background: rgba(255,255,255,0.15);
        border:1px solid rgba(31,59,77,0.5);
        padding:8px 16px;
        border-radius:999px;
        cursor:pointer;
        color: var(--text);
        font-weight:600;
        transition: var(--transition);
        backdrop-filter: blur(6px);
      }
      .todo-filters button.active {
        background: var(--accent);
        color:#fff;
        border-color: var(--accent);
      }
      .todo-grid {
        display:grid;
        gap:1.25rem;
        grid-template-columns: repeat(auto-fit,minmax(240px,1fr));
      }
      .todo-card {
        position:relative;
        background: var(--card);
        border-radius: var(--radius);
        padding:1.25rem 1.5rem;
        box-shadow: var(--shadow);
        display:flex;
        flex-direction:column;
        transition: var(--transition);
        min-height:170px;
        opacity:0;
        transform: translateY(15px);
      }
      .todo-card.visible { opacity:1; transform:translateY(0); }
      .todo-card:hover { transform: translateY(-3px); }
      .todo-card h3 { margin:0 0 6px; font-family: var(--heading); font-size:1.2rem; }
      .todo-card small { margin-top:auto; font-size:0.65rem; opacity:.75; }

      .save-btn {
        position: absolute;
        top: 12px;
        right: 12px;
        background: rgba(255,255,255,0.95);
        border-radius:50%;
        width:32px;
        height:32px;
        display:flex;
        align-items:center;
        justify-content:center;
        cursor:pointer;
        border:1px solid rgba(31,59,77,0.4);
        transition: var(--transition);
        padding:0;
      }
      .save-btn svg {
        width: 16px;
        height: 16px;
        stroke: var(--accent);
        fill: none;
        transition: var(--transition);
      }
      .save-btn.saved {
        background: var(--accent);
        border-color: var(--accent);
      }
      .save-btn.saved svg {
        stroke: white;
        fill: white;
      }

      /* Itinerary toolbar */
      .itinerary-toolbar {
        position: fixed;
        bottom: 20px;
        right: 20px;
        background: #fff;
        border-radius: 14px;
        padding: 14px 18px;
        box-shadow: 0 40px 100px -10px rgba(31,59,77,0.3);
        display: flex;
        gap: 16px;
        align-items: flex-start;
        font-size: 0.85rem;
        z-index: 1200;
        max-width: 380px;
        flex-wrap: wrap;
        border:1px solid rgba(31,59,77,0.3);
      }
      .saved-list {
        display:flex;
        flex-wrap:wrap;
        gap:6px;
        margin-bottom:4px;
      }
      .saved-pill {
        background: rgba(31,59,77,0.1);
        padding: 4px 10px;
        border-radius:999px;
        font-size:0.6rem;
        display:flex;
        align-items:center;
        gap:4px;
      }
      .itinerary-actions {
        display:flex;
        gap:8px;
        flex-wrap:wrap;
      }
      .itinerary-actions button {
        background: var(--accent);
        border:none;
        color:white;
        padding:6px 12px;
        border-radius:999px;
        cursor:pointer;
        font-size:0.7rem;
        font-weight:600;
        transition: var(--transition);
      }
      .itinerary-actions button.secondary {
        background: #666;
      }
      @keyframes pill-bounce {
        0% { transform: scale(1); }
        30% { transform: scale(1.15); }
        60% { transform: scale(0.95); }
        100% { transform: scale(1); }
      }
      .saved-pill.bounce { animation: pill-bounce 0.6s ease; }

      .memory {
        display:grid;
        gap:24px;
        grid-template-columns: repeat(auto-fit,minmax(250px,1fr));
        margin:1rem 0 2rem;
      }
      .memory-text {
        background: var(--card);
        border-radius: var(--radius);
        padding:1.5rem;
        position: relative;
        box-shadow: var(--shadow);
      }
      .memory-text h3 {
        margin-top:0;
        font-family: var(--heading);
        font-size:1.6rem;
      }
      .memory-text p {
        margin:0.5rem 0 0;
        font-size:0.95rem;
      }

      .itinerary-box {
        background: var(--card);
        border-radius: var(--radius);
        padding:1.5rem 1.75rem;
        display:flex;
        flex-direction:column;
        gap:14px;
        box-shadow: var(--shadow);
        position:relative;
      }
      .itinerary-box h2 {
        margin:0;
        font-family: var(--heading);
        font-size:2rem;
      }
      .field {
        flex:1;
        min-width:140px;
        display:flex;
        flex-direction:column;
      }
      .field label {
        font-size:0.65rem;
        font-weight:600;
        margin-bottom:4px;
        text-transform:uppercase;
        letter-spacing:0.8px;
        color:#666;
      }
      .field select,
      .field input {
        padding:10px 12px;
        border-radius:8px;
        border:1px solid #ddd;
        font-size:0.95rem;
        outline:none;
      }
      .btn {
        background: var(--accent);
        color:white;
        border:none;
        padding:12px 18px;
        border-radius:999px;
        cursor:pointer;
        font-weight:600;
        font-size:1rem;
        transition: var(--transition);
      }
      .btn:hover { filter:brightness(1.05); }

      @media (max-width:960px) {
        .grid { grid-template-columns:1fr; }
        .memory { grid-template-columns:1fr; }
      }
    </style>
  </head>

  <body>
    <a class="back-btn" href="/">← Back</a>

    <div class="container">
      <!-- Mini TOC -->
      <div class="mini-nav" aria-label="section navigation">
        <a href="#best-time" data-target="best-time">Best Time</a>
        <a href="#dont-miss" data-target="dont-miss">Don't Miss</a>
        <a href="#postcards" data-target="postcards">Postcards</a>
        <a href="#memory" data-target="memory">Glasgow Memory</a>
        <a href="#things-to-do" data-target="things-to-do">Things to Do</a>
        <a href="#best-views" data-target="best-views">Best Views</a>
        <a href="#cafes" data-target="cafes">Aesthetic Cafés</a>
        <a href="#nightlife" data-target="nightlife">Nightlife</a>
        <a href="#offbeat" data-target="offbeat">Offbeat</a>
        <a href="#itinerary" data-target="itinerary">Custom Itinerary</a>
      </div>

      <!-- Hero / Intro -->
      <section class="hero" id="hero">
        <div class="ambient" aria-hidden="true">
          <div class="drop" style="top:12%; left:22%; animation-delay:0s;"></div>
          <div class="drop" style="top:65%; left:72%; animation-delay:5s;"></div>
          <div class="drop" style="top:78%; left:38%; animation-delay:9s;"></div>
        </div>

        <h1>Glasgow</h1>
        <div class="subtitle">
          Rough-hewn warmth, music pulsing from corners, and a city that’s proudly itself—industrial
          bones softened by creativity and conversation.
        </div>
        <div class="quote">
          “Glasgow wears its heart on its sleeve; the louder the city, the warmer the welcome.” <small>— Local whisper</small>
        </div>
        <div class="quote">
          “Every street has a beat—listen and you’ll find the story.” <small>— Ayushi</small>
        </div>
      </section>

      <!-- Best Time to Visit -->
      <section class="section" id="best-time">
        <h2>Best Time to Visit</h2>
        <div class="desc">
          Glasgow’s texture shifts with the seasons—bright spring brings blooming parks, summer pulses
          with festivals and street life, autumn layers the city in soft amber light, and winter is
          intimate with cozy pubs and live music. There’s no single “perfect” moment; each visit feels
          different. If you’re coming for the first time, late spring (May–June) or early autumn
          (September) gives you milder weather, local events, and the city showing up with energy
          without the full summer bustle.
        </div>
        <div class="card" style="max-width:850px; margin:0 auto; display:flex; gap:20px; flex-wrap:wrap;">
          <div style="flex:1; min-width:220px;">
            <div class="label">Why First-Timers</div>
            <h3 style="margin-top:0; font-size:1.35rem;">Inventive & Approachable</h3>
            <p>
              The weather is forgiving, cultural events are live, and the city’s creative pulse is easy
              to feel—ideal for feeling under the skin of Glasgow without needing to rush.
            </p>
          </div>
          <div style="flex:1; min-width:220px;">
            <div class="label">Seasonal Notes</div>
            <h3 style="margin-top:0; font-size:1.35rem;">Always layered</h3>
            <p>
              Rain can surprise, but it’s part of the rhythm; winter evenings glow, and summer spills
              into long, music-soaked nights. Glasgow invites repeat discovery.
            </p>
          </div>
        </div>
      </section>

      <!-- Don't Miss -->
      <section class="section" id="dont-miss">
        <h2>Don't Miss</h2>
        <div class="desc">
          Core footholds a Glaswegian would point you toward—places where the city’s history, music,
          design, and people intersect in the most immediate way.
        </div>
        <div class="pill-list">
          <div class="pill">Kelvingrove Art Gallery & Museum</div>
          <div class="pill">Glasgow Cathedral & Necropolis</div>
          <div class="pill">Riverside Museum</div>
          <div class="pill">The Lighthouse</div>
          <div class="pill">West End & Botanic Gardens</div>
        </div>
        <div class="grid" style="grid-template-columns:repeat(auto-fit,minmax(240px,1fr)); margin-top:8px;">
          <div class="card" data-id="kelvingrove">
            <div class="save-btn" aria-label="save Kelvingrove">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <div class="label">Cultural Anchor</div>
            <h3>Kelvingrove Art Gallery & Museum</h3>
            <p>
              Eclectic collections in a grand sandstone hall—art, natural history, and surprise moments
              that feel both learned and accessible.
            </p>
          </div>
          <div class="card" data-id="cathedral-necropolis">
            <div class="save-btn" aria-label="save Glasgow Cathedral">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <div class="label">Historic</div>
            <h3>Glasgow Cathedral & Necropolis</h3>
            <p>
              Gothic reverence and the hill of the Necropolis—climb for the view, linger for the stories
              carved in stone.
            </p>
          </div>
          <div class="card" data-id="riverside-museum">
            <div class="save-btn" aria-label="save Riverside Museum">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <div class="label">Design & Motion</div>
            <h3>Riverside Museum</h3>
            <p>
              Transport history presented with playful, modern staging—ideal for wandering and connecting
              past movement with present energy.
            </p>
          </div>
          <div class="card" data-id="the-lighthouse">
            <div class="save-btn" aria-label="save The Lighthouse">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <div class="label">Design View</div>
            <h3>The Lighthouse</h3>
            <p>
              Scotland’s Centre for Design—climb the tower for a city panorama, and absorb creative whispers
              inside.
            </p>
          </div>
          <div class="card" data-id="botanic-west-end">
            <div class="save-btn" aria-label="save Botanic Gardens">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <div class="label">Green & Quiet</div>
            <h3>West End & Botanic Gardens</h3>
            <p>
              Leafy walks, glasshouses, and the gentle hum of a neighborhood that balances calm with character.
            </p>
          </div>
        </div>
      </section>

      <!-- Postcards -->
      <section class="section" id="postcards">
        <h2>Postcards</h2>
        <div class="desc">
          Small, unscripted impressions from Glasgow—moments where the city felt like a friend rather than a destination.
        </div>
        <div class="postcards">
          <div class="flip-card">
            <div class="flip-inner">
              <div class="flip-front">
                <img src="/images/glasgow-river-dusk.jpg" alt="River Clyde Dusk" />
                <div class="overlay">
                  <p class="title" style="margin:0;">Riverside Reflections</p>
                  <p class="note" style="margin:4px 0 0; font-size:0.7rem;">
                    Light softening over the Clyde, bridge hum and distant music blending into one slow beat.
                  </p>
                </div>
              </div>
              <div class="flip-back">
                <div class="title">Riverside Reflections</div>
                <div class="note">
                  We paused with takeaway coffee, watching ferries drift—Glasgow felt like something in the
                  midst of becoming, and already familiar. —Purvi
                </div>
              </div>
            </div>
          </div>

          <div class="flip-card">
            <div class="flip-inner">
              <div class="flip-front">
                <img src="/images/glasgow-willow-tea.jpg" alt="Willow Tea Rooms" />
                <div class="overlay">
                  <p class="title" style="margin:0;">Mackintosh Pause</p>
                  <p class="note" style="margin:4px 0 0; font-size:0.7rem;">
                    Tea, carved details, and a sense that design and everyday life are inseparable here.
                  </p>
                </div>
              </div>
              <div class="flip-back">
                <div class="title">Mackintosh Pause</div>
                <div class="note">
                  Sipping something warm while lines curved gently around us—Glasgow’s quiet artistry revealed
                  itself in the small ritual. —Ayushi
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Glasgow Memory -->
      <section class="section" id="memory">
        <h2>Glasgow Memory</h2>
        <div class="desc">
          Unsanctioned rhythms—late-night busking, accidentally finding a mural that felt like a secret, and
          the warmth of a whiskey poured during a drizzle.
        </div>
        <div class="memory">
          <div class="flip-card" style="max-width:320px;">
            <div class="flip-inner">
              <div class="flip-front">
                <img src="/images/glasgow-street-art.jpg" alt="Street Art" />
                <div class="overlay">
                  <p class="title" style="margin:0;">Hidden Mural</p>
                  <p class="note" style="margin:4px 0 0; font-size:0.7rem;">
                    Colors and faces unexpectedly tucked behind alleys—felt like the city nudging us to look closer.
                  </p>
                </div>
              </div>
              <div class="flip-back">
                <div class="title">Hidden Mural</div>
                <div class="note">
                  Wandering without a plan, we stumbled into a wall that stopped us—Glasgow giving texture
                  for free. —Purvi
                </div>
              </div>
            </div>
          </div>

          <div class="memory-text">
            <h3>Drizzle & Whiskey</h3>
            <p>
              A quick detour into a narrow bar turned into a long conversation—rain pattering outside while
              the owner poured stories with the whiskey. The best parts weren’t scheduled.
            </p>
            <p style="margin-top:8px; font-style:italic;">
              “This city is loud in all the right ways.” — Ayushi
            </p>
          </div>

          <div class="flip-card" style="max-width:320px;">
            <div class="flip-inner">
              <div class="flip-front">
                <img src="/images/glasgow-live-music.jpg" alt="Live Music" />
                <div class="overlay">
                  <p class="title" style="margin:0;">Late Night Pulse</p>
                  <p class="note" style="margin:4px 0 0; font-size:0.7rem;">
                    The laughter, the beat, and a room that felt like a collective breath—music threaded through the bones.
                  </p>
                </div>
              </div>
              <div class="flip-back">
                <div class="title">Late Night Pulse</div>
                <div class="note">
                  From small venues to neon-lit alleys, the city kept playing—Glasgow’s soundtrack became ours for a night. —Purvi & Ayushi
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Things to Do -->
      <section class="section" id="things-to-do">
        <h2>Things to Do</h2>
        <div class="desc">
          Local-tested layers—start with the markers of identity, then drift into corners that feel like
          personal discoveries. Everything here feeds into a well-curated Glasgow itinerary (free for you).
        </div>

        <div class="todo-filters" aria-label="filters for things to do">
          <button data-filter="all" class="active">All</button>
          <button data-filter="must">Must Visit</button>
          <button data-filter="views">Best Views</button>
          <button data-filter="cafes">Cafés</button>
          <button data-filter="nightlife">Nightlife</button>
          <button data-filter="offbeat">Offbeat</button>
        </div>

        <div class="todo-grid">
          <!-- Must Visit -->
          <div class="todo-card" data-category="must" data-id="kelvingrove">
            <div class="save-btn" aria-label="save Kelvingrove">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Kelvingrove Art Gallery</h3>
            <p>Grand eclecticism in a cathedral of culture—art and curiosity collide.</p>
            <small>Museum</small>
          </div>
          <div class="todo-card" data-category="must" data-id="cathedral-necropolis">
            <div class="save-btn" aria-label="save Cathedral & Necropolis">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Cathedral & Necropolis</h3>
            <p>Historic reverence and sweeping views—Glasgow’s past framed above the city.</p>
            <small>Heritage</small>
          </div>
          <div class="todo-card" data-category="must" data-id="riverside-museum">
            <div class="save-btn" aria-label="save Riverside Museum">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Riverside Museum</h3>
            <p>Playful transport history—design-forward and unexpectedly joyful.</p>
            <small>Interactive</small>
          </div>

          <!-- Best Views -->
          <div class="todo-card" data-category="views" data-id="necropolis-view">
            <div class="save-btn" aria-label="save Necropolis View">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Necropolis Overlook</h3>
            <p>City spread below, stones and stories above—one of the most quietly powerful panoramas.</p>
            <small>Vantage</small>
          </div>
          <div class="todo-card" data-category="views" data-id="the-lighthouse-view">
            <div class="save-btn" aria-label="save Lighthouse View">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>The Lighthouse</h3>
            <p>Elevated design perspective—see the city and sense its creative layers.</p>
            <small>City view</small>
          </div>

          <!-- Aesthetic Cafés -->
          <div class="todo-card" data-category="cafes" data-id="willow-tea-rooms">
            <div class="save-btn" aria-label="save Willow Tea Rooms">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Willow Tea Rooms</h3>
            <p>Charles Rennie Mackintosh elegance—sipping tea becomes a design appreciation ritual.</p>
            <small>Heritage café</small>
          </div>
          <div class="todo-card" data-category="cafes" data-id="papercup">
            <div class="save-btn" aria-label="save Papercup Coffee">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Papercup Coffee Company</h3>
            <p>Minimal, warm, and cleverly curated—great beans, quiet corners, and local rhythm.</p>
            <small>Modern</small>
          </div>

          <!-- Nightlife -->
          <div class="todo-card" data-category="nightlife" data-id="king-tuts">
            <div class="save-btn" aria-label="save King Tut's Wah Wah Hut">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>King Tut's Wah Wah Hut</h3>
            <p>Iconic live music venue—new talent and local legends share the stage in a compact, electric space.</p>
            <small>Music</small>
          </div>
          <div class="todo-card" data-category="nightlife" data-id="barrowland">
            <div class="save-btn" aria-label="save Barrowland Ballroom">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Barrowland Ballroom</h3>
            <p>Historic dancefloor and performance space—immersive, slightly gritty, and unforgettable.</p>
            <small>Atmospheric</small>
          </div>

          <!-- Offbeat -->
          <div class="todo-card" data-category="offbeat" data-id="sharmanka">
            <div class="save-btn" aria-label="save Sharmanka">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>Sharmanka Kinetic Gallery</h3>
            <p>Mechanical sculptures telling strange stories—unexpected, moving, and oddly poetic.</p>
            <small>Quirky art</small>
          </div>
          <div class="todo-card" data-category="offbeat" data-id="hidden-lane">
            <div class="save-btn" aria-label="save Hidden Lane">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
            <h3>The Hidden Lane</h3>
            <p>An offbeat cluster of independent makers, cafes, and surprise installations—local creativity off the usual path.</p>
            <small>Local secret</small>
          </div>
        </div>
      </section>

      <!-- Custom Itinerary -->
      <section class="section" id="itinerary">
        <h2>Your Custom Glasgow Itinerary</h2>
        <div class="desc">
          Handcrafted from what you’ve saved—“This is your Glasgow mini-itinerary for today,”
          numbered and recommended by Natsu & Kasii. Well-curated, personalized, and completely
          free. Pick your vibe, group size, and dates; we’ll package it for you via WhatsApp.
        </div>
        <div class="itinerary-box">
          <div style="display:flex; gap:18px; flex-wrap:wrap;">
            <div class="field">
              <label for="vibe">Vibe</label>
              <select id="vibe">
                <option value="urban">Urban</option>
                <option value="music">Music & Night</option>
                <option value="heritage">Heritage</option>
                <option value="creative">Creative</option>
                <option value="mixed">Mixed</option>
              </select>
            </div>
            <div class="field">
              <label for="people">Group Size</label>
              <input type="number" id="people" min="1" value="2" />
            </div>
            <div class="field">
              <label for="dates">Dates (optional)</label>
              <input type="text" id="dates" placeholder="e.g., 8–12 Jun" />
            </div>
          </div>
          <div>
            <p style="margin:6px 0;">
              We’ll auto-generate a tidy numbered itinerary based on what you’ve saved and the vibe
              you choose. Shareable, light, and free—no signup needed.
            </p>
            <button class="btn" id="whatsapp-glasgow">Send on WhatsApp</button>
          </div>
        </div>
      </section>
    </div>

    <!-- Toolbar -->
    <div id="itinerary-toolbar" class="itinerary-toolbar" style="display:none;">
      <div style="flex:1; min-width:140px;">
        <h4>Your Glasgow mini-itinerary</h4>
        <div id="itinerary-summary" style="margin-bottom:6px; font-size:0.9rem; line-height:1.2;"></div>
        <div class="saved-list" id="saved-list"></div>
        <div style="font-size:0.6rem; color:#555;">
          Build, copy, or share what you’ve saved.
        </div>
      </div>
      <div class="itinerary-actions">
        <button id="copy-link">Copy Link</button>
        <button id="share-whatsapp">Share on WhatsApp</button>
        <button id="share-email" class="secondary">Email</button>
        <button id="clear-itinerary" class="secondary">Clear</button>
      </div>
    </div>

    <script>
      const DISPLAY_NAMES = {
        "kelvingrove": "Kelvingrove Art Gallery",
        "cathedral-necropolis": "Glasgow Cathedral & Necropolis",
        "riverside-museum": "Riverside Museum",
        "the-lighthouse": "The Lighthouse",
        "botanic-west-end": "West End & Botanic Gardens",
        "necropolis-view": "Necropolis Overlook",
        "the-lighthouse-view": "The Lighthouse View",
        "willow-tea-rooms": "Willow Tea Rooms",
        "papercup": "Papercup Coffee Company",
        "king-tuts": "King Tut's Wah Wah Hut",
        "barrowland": "Barrowland Ballroom",
        "sharmanka": "Sharmanka Kinetic Gallery",
        "hidden-lane": "The Hidden Lane"
      };
      function getDisplayName(id) {
        return DISPLAY_NAMES[id] || id.replace(/[-_]/g, " ");
      }

      const STORAGE_KEY = "glasgow-saved-items";
      let previousSaved = [];

      function getSaved() {
        try {
          const raw = localStorage.getItem(STORAGE_KEY);
          return raw ? JSON.parse(raw) : [];
        } catch { return []; }
      }
      function setSaved(arr) {
        localStorage.setItem(STORAGE_KEY, JSON.stringify(arr));
        refreshToolbar();
      }
      function toggleSaved(id) {
        let saved = getSaved();
        if (saved.includes(id)) saved = saved.filter(s => s !== id);
        else saved.push(id);
        setSaved(saved);
        updateCardState();
      }
      function updateCardState() {
        const saved = getSaved();
        document.querySelectorAll(".save-btn").forEach(btn => {
          const parent = btn.closest("[data-id]");
          if (!parent) return;
          const id = parent.dataset.id;
          if (saved.includes(id)) btn.classList.add("saved");
          else btn.classList.remove("saved");
        });
      }
      function updateSummary() {
        const summaryEl = document.getElementById("itinerary-summary");
        const saved = getSaved();
        if (!saved.length) {
          summaryEl.textContent = "";
          return;
        }
        const names = saved.map((id, idx) => `${idx + 1}. ${getDisplayName(id)}`);
        const baseUrl = `${window.location.origin}${window.location.pathname}`;
        const param = encodeURIComponent(saved.join(","));
        const shareLink = `${baseUrl}?saved=${param}`;
        summaryEl.innerHTML = `
          <strong>Today's Glasgow itinerary:</strong> ${names.join(" · ")}.<br/>
          Recommended by <em>Natsu & Kasii</em>. 
          <a href="${shareLink}" style="color: var(--accent); text-decoration:underline;">View / share this</a>
        `;
      }
      function refreshToolbar() {
        const toolbar = document.getElementById("itinerary-toolbar");
        const saved = getSaved();
        const savedList = document.getElementById("saved-list");
        savedList.innerHTML = "";
        if (!saved.length) {
          toolbar.style.display = "none";
          previousSaved = [];
          updateSummary();
          return;
        }
        toolbar.style.display = "flex";
        const newlyAdded = saved.filter(id => !previousSaved.includes(id));
        saved.forEach(id => {
          const pill = document.createElement("div");
          pill.className = "saved-pill";
          pill.textContent = getDisplayName(id);
          const x = document.createElement("span");
          x.style.marginLeft = "6px";
          x.style.cursor = "pointer";
          x.textContent = "×";
          x.addEventListener("click", () => toggleSaved(id));
          pill.appendChild(x);
          if (newlyAdded.includes(id)) {
            pill.classList.add("bounce");
            pill.addEventListener("animationend", () => pill.classList.remove("bounce"));
          }
          savedList.appendChild(pill);
        });
        previousSaved = [...saved];
        updateSummary();
      }

      document.addEventListener("DOMContentLoaded", () => {
        // progressive reveal
        const observer = new IntersectionObserver((entries) => {
          entries.forEach(e => {
            if (e.isIntersecting) {
              e.target.classList.add("visible");
              observer.unobserve(e.target);
            }
          });
        }, { threshold: 0.15 });
        document.querySelectorAll(".card, .todo-card").forEach(el => observer.observe(el));

        // mini-TOC highlight
        const sectionObserver = new IntersectionObserver((entries) => {
          entries.forEach(entry => {
            const id = entry.target.id;
            const link = document.querySelector(`.mini-nav a[data-target="${id}"]`);
            if (link && entry.isIntersecting) {
              document.querySelectorAll(".mini-nav a").forEach(a => a.classList.remove("active"));
              link.classList.add("active");
            }
          });
        }, { rootMargin: "-40% 0px -50% 0px" });
        document.querySelectorAll(".section").forEach(s => sectionObserver.observe(s));

        // save listeners
        document.querySelectorAll(".save-btn").forEach(btn => {
          btn.addEventListener("click", (e) => {
            e.stopPropagation();
            const card = btn.closest("[data-id]");
            if (!card) return;
            toggleSaved(card.dataset.id);
          });
        });

        // filters
        document.querySelectorAll(".todo-filters button").forEach(b => {
          b.addEventListener("click", () => {
            document.querySelectorAll(".todo-filters button").forEach(x => x.classList.remove("active"));
            b.classList.add("active");
            const filter = b.dataset.filter;
            document.querySelectorAll(".todo-card").forEach(card => {
              if (filter === "all" || card.dataset.category === filter) card.style.display="flex";
              else card.style.display="none";
            });
          });
        });

        // restore saved from URL
        const params = new URLSearchParams(window.location.search);
        if (params.has("saved")) {
          try {
            const incoming = decodeURIComponent(params.get("saved") || "");
            const list = incoming.split(",").filter(Boolean);
            setSaved(Array.from(new Set([...getSaved(), ...list])));
          } catch {}
        } else {
          refreshToolbar();
        }
        updateCardState();
      });

      // share / copy
      document.getElementById("copy-link").addEventListener("click", () => {
        const saved = getSaved();
        if (!saved.length) return;
        const param = encodeURIComponent(saved.join(","));
        const url = `${window.location.origin}${window.location.pathname}?saved=${param}`;
        const names = saved.map((id, idx) => `${idx+1}. ${getDisplayName(id)}`).join("\n");
        const text = `Today's Glasgow itinerary:\n${names}\n\nRecommended by Natsu & Kasii\n${url}`;
        navigator.clipboard.writeText(text);
        alert("Itinerary copied!");
      });
      document.getElementById("share-whatsapp").addEventListener("click", () => {
        const saved = getSaved();
        if (!saved.length) return;
        const baseUrl = `${window.location.origin}${window.location.pathname}`;
        const param = encodeURIComponent(saved.join(","));
        const shareLink = `${baseUrl}?saved=${param}`;
        const names = saved.map((id, idx) => `${idx+1}. ${getDisplayName(id)}`).join("\n");
        const summary = `My Glasgow mini-itinerary:\n${names}\n\nRecommended by Natsu & Kasii\n${shareLink}`;
        window.open(`https://wa.me/?text=${encodeURIComponent(summary)}`, "_blank");
      });
      document.getElementById("share-email").addEventListener("click", () => {
        const saved = getSaved();
        if (!saved.length) return;
        const param = encodeURIComponent(saved.join(","));
        const url = `${window.location.origin}${window.location.pathname}?saved=${param}`;
        window.location.href = `mailto:?subject=My Glasgow Mini-Itinerary&body=${encodeURIComponent("Here's what I saved:\n" + url)}`;
      });
      document.getElementById("clear-itinerary").addEventListener("click", () => {
        localStorage.removeItem(STORAGE_KEY);
        updateCardState();
        refreshToolbar();
      });

      // WhatsApp custom itinerary
      document.getElementById("whatsapp-glasgow").addEventListener("click", () => {
        const vibe = document.getElementById("vibe").value;
        const people = document.getElementById("people").value;
        const dates = document.getElementById("dates").value;
        const saved = getSaved();
        const namesList = saved.map((id, idx) => `${idx+1}. ${getDisplayName(id)}`).join("\n");
        let msg = `This is your Glasgow mini-itinerary for today:\n${namesList}\n\nVibe: ${vibe}\nGroup size: ${people}`;
        if (dates.trim()) msg += `\nDates: ${dates}`;
        msg += `\nRecommended by Natsu & Kasii.`;
        const phone = "1234567890"; // replace with real number
        window.open(`https://wa.me/${phone}?text=${encodeURIComponent(msg)}`, "_blank");
      });
    </script>
  </body>
</html>
